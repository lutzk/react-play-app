const HOME = 'HOME';
const LOGIN = 'LOGIN';
const SOL = 'SOL_VIEW';
const ROVER_VIEW = 'ROVER_VIEW';

const PATHS = {
  // [ROOT]: '/',
  [SOL]: '/sol',
  [HOME]: '/home',
  [LOGIN]: '/login',
  [ROVER_VIEW]: '/rover-view',
};


export {
  SOL,
  HOME,
  PATHS,
  LOGIN,
  ROVER_VIEW,
};
